package my.interfaces.decouple.filters;


public class Test {
    public static void main(String[] args) {
        // 不能复用 apply() 方法
        // WaveForm w = new WaveForm();
        // Applicator.apply(new LowPass(1.0), w);
    }
}
